{
  "//": [
    "NOTHING SECRET GOES IN THIS FILE — it is committed.",
    "SQL password  -> SQL_PASSWORD environment variable (only needed for SQL auth).",
    "SMTP password -> SMTP_PASSWORD environment variable.",
    "JWT key       -> dotnet user-secrets set \"Jwt:Key\" \"<64+ chars>\"",
    "",
    "Jwt:Key, Issuer and Audience MUST match JP.Sso.Api — that API issues the",
    "tokens this one validates.",
    "",
    "JP.App.Api configures Mdm and App. It has no connection to jp_sso: user",
    "identity reaches it through the signed token, not a database read."
  ],

  "ConnectionStrings": {
    "//": "Server and database only. Encryption, timeouts and Application Name are applied by DbConnectionFactory from the Database section below.",
    "//Sso": "Phase 2D. This API writes to jp_sso when an approval completes — it activates the user and grants the role before creating the school. Cross-database orchestration lives in the API layer (decision 2.2), so this API needs a connection to every database it orchestrates, not just its own. Without it the approval commits and activation throws 'no connection string for the Sso database' — which is exactly the partial-completion case the orchestrator reports rather than hides.",
    "Sso": "Server=localhost\\TARUN;Database=jp_sso;Integrated Security=true",
    "Mdm": "Server=localhost\\TARUN;Database=jp_mdm;Integrated Security=true",
    "App": "Server=localhost\\TARUN;Database=jp_app;Integrated Security=true"
  },

  "Database": {
    "//": [
      "Encrypt defaults to true in Microsoft.Data.SqlClient 4.0+.",
      "TrustServerCertificate MUST stay false here — appsettings.Development.json",
      "turns it on for localhost\\TARUN, which presents a self-signed certificate."
    ],
    "Encrypt": true,
    "TrustServerCertificate": false,
    "ConnectTimeoutSeconds": 15,
    "CommandTimeoutSeconds": 30,
    "MaxRetryAttempts": 3,
    "RetryBaseDelayMs": 200
  },

  "Jwt": {
    "//": "Key is deliberately absent. Set it in user-secrets or an environment variable.",
    "Issuer": "jp-sso-api",
    "Audience": "jp-portal",
    "AccessTokenMinutes": 60,
    "RefreshTokenDays": 7,
    "ClockSkewSeconds": 30
  },

  "FileStorage": {
    "RootPath": "App_Data/uploads",
    "MaxFileSizeKb": 10240,
    "AllowedExtensions": [ ".pdf", ".jpg", ".jpeg", ".png", ".webp", ".doc", ".docx" ]
  },

  "Smtp": {
    "Enabled": false,
    "Host": "",
    "Port": 587,
    "Security": "StartTls",
    "UserName": "",
    "FromAddress": "no-reply@teacherportal.local",
    "FromName": "Teacher Recruitment Portal",
    "DropFolder": "App_Data/mail-drop",
    "TimeoutSeconds": 30
  },

  "Cors": {
    "//": [
      "One origin per app (decision 2.17). These are exact-match, so a dev",
      "server that lands on a different port is refused — which is the loud",
      "failure we want rather than a silent one."
    ],
    "AllowedOrigins": [
      "http://localhost:4200",
      "http://localhost:4300",
      "http://localhost:4400",
      "http://localhost:4500"
    ]
  },

  "Serilog": {
    "Using": [ "Serilog.Sinks.Console", "Serilog.Sinks.File" ],
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.Hosting.Lifetime": "Information",
        "System.Net.Http.HttpClient": "Warning"
      }
    },
    "WriteTo": [
      {
        "Name": "Console",
        "Args": {
          "outputTemplate": "[{Timestamp:HH:mm:ss} {Level:u3}] {Message:lj} {Properties:j}{NewLine}{Exception}"
        }
      },
      {
        "Name": "File",
        "Args": {
          "path": "App_Data/logs/app-.log",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 14,
          "outputTemplate": "{Timestamp:o} [{Level:u3}] {Message:lj} {Properties:j}{NewLine}{Exception}"
        }
      }
    ],
    "Enrich": [ "FromLogContext" ]
  },

  "AllowedHosts": "*"
}
