{
  "//setup": [
    "Copy this file to appsettings.Development.json — the real one is gitignored.",
    "It holds no secrets; the JWT key is in user-secrets and the SQL password in",
    "the SQL_PASSWORD environment variable. It is ignored so that a machine-specific",
    "connection string or a local override never lands in the repository."
  ],
  "//": [
    "Local development overrides.",
    "SMTP stays disabled: rendered emails are written to App_Data/mail-drop as",
    ".eml files instead of being sent."
  ],

  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information",
        "Microsoft.AspNetCore.Hosting.Diagnostics": "Warning",
        "Microsoft.AspNetCore.Routing": "Warning"
      }
    }
  },

  "Database": {
    "//": "localhost\\TARUN has a self-signed certificate. Traffic stays encrypted; only the trust check is skipped. NEVER set this in a real environment.",
    "TrustServerCertificate": true
  },

  "Smtp": {
    "Enabled": false
  }
}
